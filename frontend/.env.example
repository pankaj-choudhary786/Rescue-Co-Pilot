# Last-Minute Lifesaver Frontend Environment Variables
# Since we are using a full-stack unified origin (Express proxies backend /api requests),
# client-side VITE_ variables are optional, but you can declare custom API URLs here if needed.
VITE_API_URL=
